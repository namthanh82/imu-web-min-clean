[Unit]
Description=ReTrack Flask Backend
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/retrack
ExecStart=/opt/retrack/run_server.sh
Restart=always
RestartSec=3
Environment=PORT=5000
Environment=RETRACK_RUNTIME=server

[Install]
WantedBy=multi-user.target
